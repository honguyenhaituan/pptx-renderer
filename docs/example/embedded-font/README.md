# Embedded font example

`source.pptx` declares and embeds regular and bold faces of **Be Vietnam Pro**.
It verifies that the renderer uses fonts carried by the presentation without
requiring the host application or operating system to install them.

The embedded font is distributed under the SIL Open Font License 1.1; see
`OFL.txt`.
